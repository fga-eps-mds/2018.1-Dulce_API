version: "3.5"
services:
  loginmicroservice:
    build: .
    command:  bash -c "npm install; nodemon app.js --color=always"
    ports:
     - "8081:8080"
     - "5859:5858"
    volumes:
     - ./app:/usr/src/app
    networks:
     - mongo_gabriela
networks:
  mongo_gabriela:
    external:
      name: my_network
